<!-- Loop over each object in the projects array -->
{{#each posts as |post|}}
  <div class='row mb-4 post'>
    <a href='/post/{{this.id}}'><h2>{{post.name}}</h2></a>
    <div class='col-md-5'>
      <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
    </div>
    <div class='col-md-7'>
      <p>
        {{post.content}}
      </p>
    </div>
  </div>
{{/each}}